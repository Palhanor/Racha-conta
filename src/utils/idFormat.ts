export const idSize = 16;
// export const alphabet = "0123456789abcdefghijklmnopqrstuvwxyz";

// export const formatarID = (nome: string, id: string): string => {
//   return `${nome
//     .normalize("NFD")
//     .replace(/[\u0300-\u036f]/g, "")
//     .toLowerCase()
//     .replace(/\s/g, "-")}_${id}`;
// };
